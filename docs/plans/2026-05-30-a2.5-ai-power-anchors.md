# A2.5 — AI-电力 L3 缺口补全（multi_segment_anchors）

**Status**: 本地完成 + 已验证 + Codex P1 已修，**未 push / 未落库 / 未 merge**（gated on Boss）
**Date**: 2026-05-30
**Commits (reviewable unit)**: `d6319d5`（anchors）+ `a5844af`（manifest schema fix, Codex P1）  ·  base `main` = `2fc425b`  ·  branch `feature/concept-l3-ai-power-anchors`
**Related**: A2 落库（commit `5b5c631`）· issue 030（coverage mismatch）· A3（周频 concept-build cron，未来 `--reclassify`）

---

## 1. 触发与第一性原理结论

A2.5 原定范围是"老 CSV → 新票 L3 补全"，假设 249+ rule 行的空 L3 是分类失败。**实证推翻了这个前提**：

- 合并 967 行里 617 行无 L3（547 rule + 66 llm + 4 manual），但 L3 是**稀疏叙事覆盖**，不是必填字段（即使 LLM 也有 16% 留空）。
- Boss 把范围收窄为"**确保科技/半导体/AI 相关的都有 L3**"。
- 数据显示该子集**已 100% 覆盖**：
  - 半导体 40/40 全有 L3（`半导体周期;AI算力` 等）。
  - 结构性保证：`industry_map`（82 对）里**没有任何半导体/Technology/Software 条目** → 所有半导体/AI 工业代码必然 fall-through 到 LLM → 拿到 L3。rule 路径永远碰不到它们。
- **真缺口 = AI-电力链**：核能/独立电力运营商的龙头被 rule-trapped。`Utilities|Independent Power Producers` / `Utilities|Renewable Utilities` **在 industry_map 里有条目** → 走 rule → `company_concepts.py:133` 硬编码 `l3_themes=[]` → 这几个最该有"数据中心电力/核能复兴"标签的票反而被压成空。

`核能复兴` 主题原本只有 2 个成员（CCJ/OKLO），漏掉 CEG/VST/TLN；`数据中心电力`(23) 也漏掉它们。

## 2. 改动（Boss 已逐项拍板）

5 个 `multi_segment_anchors`（anchors 8 → 13，taxonomy v2.1 → **v2.2**）：

| symbol | l1 | l2 | theme_ids (L3) | business_role |
|--------|-----|-----|----------------|---------------|
| CEG | realestate_utility | power_utility | nuclear_revival, datacenter_power | 美国最大核电运营商 + 数据中心PPA |
| VST | realestate_utility | power_utility | datacenter_power, nuclear_revival | 德州商业电力 + 核电 + 数据中心负荷 |
| TLN | realestate_utility | power_utility | nuclear_revival, datacenter_power | 核电IPP + 亚马逊Susquehanna数据中心 |
| NRG | realestate_utility | power_utility | datacenter_power | 德州零售 + 商业电力 + 数据中心负荷 |
| GEV | energy_materials | clean_energy | datacenter_power, nuclear_revival, energy_transition | 燃气轮机 + 电网电气化 + SMR核电 + 风电 |

附带：CEG 的 FMP "Renewable Utilities" 误标在 anchor 里被修正为 `电力与公用`（CEG 是美国最大核电运营商，不是可再生）。

**为什么用 anchor 而不是只改 CSV**：`--read-reviewed-csv --save` 的 L3 来自 CSV，但 A3 的 `--reclassify` 会重跑 `classify()` —— 对 rule-trapped 票若无 anchor 仍返回 `[]`，会**冲掉手填 L3**。anchor 在 `classify()` 中**最高优先级**（`company_concepts.py:123` 先查 anchor），`reclassify` 路由 `rule + new manual → overwrite` 保留之 → A3 不回归。这也顺带**验证了 A3 的 anchor 处理机制**。

**Edge 2 (AES/BEP) 已评估但 Boss 决定不收**（可再生 PPe，AI-电力叙事较弱，留空合理）。

## 3. 产出文件（commit d6319d5，3 个）

- `config/concepts/concept_taxonomy_v2.json` — +5 anchors + version bump（substantive diff = 54 行）
- `reports/concept_registry/extended_pool_tags_2026-05-30.csv` — 新 dated CSV = 05-24 + **外科手术 patch 5 行**（CEG/VST/TLN/NRG/GEV：l1/l2/l3_themes/business_role/source=manual/confidence=0.99），其余 962 行**字节级不变**
- `reports/concept_registry/extended_pool_tags_2026-05-30_manifest.json` — 05-24 manifest 拷贝（同 967 universe，coverage pin 用）

> ⚠️ `git diff --stat` 把 CSV/manifest 显示成 +968/+995 是因为它们是新文件；真实可审 diff：SSOT 5 anchors + `diff 05-24.csv 05-30.csv` = 10 行。

## 4. 已完成验证

- `ConceptRegistry.classify()` 对 5 票全返回 `source=manual` + 正确 l1/l2/l3 + display_tags（如 `地产与公用 / 电力与公用 / 核能复兴`）。
- CSV 结构 diff：只有 CEG/GEV/NRG/TLN/VST 5 行变化；raw 行级 diff = 10 行（5±），其余字节级一致（已修 csv.writer CRLF→LF 以匹配原文件）。
- `--read-reviewed-csv 05-30.csv --extended-universe-path <pinned-967> --validate-only`：967 行、**0 拒绝、coverage 0**。
- 概念注册表测试：`pytest -k "concept or taxonomy or company_concept"` → **131 passed**。

## 5. Codex 审查关注点

1. anchor 的 l1/l2/theme_ids 是否都是合法 concept_id、l2.parent 是否匹配 l1（power_utility→realestate_utility ✓；clean_energy→energy_materials ✓）。
2. CEG 从 `能源与材料/新能源` 改到 `地产与公用/电力与公用` 是否合理（FMP 误标修正）。
3. CSV 外科手术是否真的只动 5 行、其余字节不变（防 csv round-trip 污染）。
4. anchor 持久性逻辑是否成立（A3 `--reclassify` 不回归）。
5. GEV 归 `能源与材料/新能源` 是否合适（设备商而非运营商；Boss 已确认按此）。

## 6. 未完成（gated）

- [ ] push origin → 云端 git pull
- [ ] 云端 `--read-reviewed-csv 05-30.csv --extended-universe-path <pinned-967> --save`（写生产 market.db）
- [ ] 云端验证 `company_concept_tags` 5 票带新 L3
- [ ] 本地 `sync_to_cloud.sh --pull`（WAL-safe + SSH `-4 -b <LAN_IP>`）+ 验证
- [ ] merge → main

## 7. 附带发现（不在本 commit，待 Boss 决策）

morning_report DV section **4 个 pre-existing 测试失败**（均在原 taxonomy 上也失败，与 A2.5 无关）：
- `test_layered_dv_renders_three_tier_concept_tags` — fixture 用已删主题 `hbm`（5/17 并入 `storage`）
- `test_missing_industry_uses_bucket_not_unclassified` — NVDA 分组标签期望 `AI算力/云`，实际 `半导体链`
- `test_dv_layered_section` — MU business_role 期望 `DRAM/HBM存储`，实际长描述
- `test_dv_section_filters_out_broad_layer` — ARM mcap 阈值过滤期望

根因：5/14–5/17 概念重构后 morning_report fixture 未更新。建议折叠进"晨报应用新概念聚类"任务（那个任务本就要重写这段渲染）。

## 8. Codex 审查结论（2026-05-30）

- **P1（已修 `a5844af`）**：05-30 manifest 是从 A2 的 05-24 sidecar 拷贝的，用 `full_universe` schema，但 `_load_review_manifest()`（:869）只读 `symbols` → 拷贝来的 manifest 解析成空集、coverage 零保护（删行静默通过）。已重生成 canonical `symbols` key（967，从 05-30 CSV 派生）。验证：loader 返回 967（原为空）；删 `03032` 后 validate-only 报 `missing 1 symbols: ['03032']`（原静默通过）；真 CSV + pin 仍 967/0 拒绝。
- **其余全过**：唯一审查 commit `d6319d5`（base `2fc425b`）；5 anchor l1/l2/theme_ids 合法、parent 匹配；CSV 真实 diff 仅 5 行；classify() 5 票 `source=manual`、A3 持久性成立；GEV 归类与 plan 一致。
- **遗留 tech debt（未修，Codex 也未要求）**：A2 的 05-24 manifest 同样是 broken schema；`full_universe`-schema 生成器与 loader 不一致。durable 修法 = 所有 manifest 写入走 `_write_review_manifest`，或 `_load_review_manifest` fallback 读 `full_universe`。Deferred。
