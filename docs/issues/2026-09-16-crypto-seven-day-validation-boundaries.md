# 7天双榜：隔离验证与浮点边界

日期：2026-09-16。状态：本次已修复并通过验证。

1. **单币异常隔离回归**：DSH重构将load(symbol)移出try，任一API/CSV失败会终止全榜。Codex审查发现并由DSH修复：捕获一次错误、三路结果明确unavailable、继续下一币；2项故障注入测试覆盖。
2. **部分目录不能保证Python导入隔离**：云端仅复制scripts/tests并在PYTHONPATH引用生产依赖，src初始化调整sys.path后加载旧生产scripts，造成17个假回归。改用完整git worktree，检查实际模块来源再验收；不修业务代码来适应错误导入路径。
3. **严格>1遇到自基准浮点误差**：云端cov/var实现将完全相同序列算成1.0000000000000002，本地恰好为1。仅在已确认方差有效且价格序列完全相同时规范为数学值1；其他序列保持原始值，1.00000001仍可入榜。跨环境测试通过。
4. **DSH输出上限不是总预算**：默认8192限制单次模型输出；第一次在任何编辑前因max-tokens退出。核查零改动后，以相同模型、新session和32768上限重试成功；不能把进程结束当实现成功。

最终本地/云端63 tests pass，真实50beta/49RS独立对拍通过；详见同日audit。

## 14天扩展验证补充
跨机器/不同numpy版本的beta浮点JSON不可逐位比较：本地与云端旧7d本就有约1e-15差异。新旧行为回归应在相同环境上运行旧commit、新代码做精确对拍；异构数学验证用明确数值误差阈值。14d扩展已用云端旧版本同环境基线确认30d/7d完全一致。
